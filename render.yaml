services:
  - type: web
    name: skillbridge-backend
    env: node
    buildCommand: npm install --prefix backend/server
    startCommand: node backend/server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000